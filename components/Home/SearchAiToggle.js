import React from "react";
import { View, Text, StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
});

const SearchAiToggle = () => {
  return (
    <View style={styles.container}>
      <Text>SearchAiToggle</Text>
    </View>
  );
};

export default SearchAiToggle;
